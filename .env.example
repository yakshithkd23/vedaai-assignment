# Google AI Studio API key (free tier). Get one at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: override the model used for extraction/grading.
# Defaults to "gemini-1.5-flash" if not set. Use a vision-capable model.
GEMINI_MODEL=gemini-1.5-flash
